<div class="container" style="margin-top: 50px;">

    <div class="card"> 
        <div class="card-header">
            <ul class="nav nav-pills w-100">
                <li class="nav-pill active">
                    <a class="nav-link">Hostel Details</a>
                </li>
                <li class="nav-pill ml-auto">
                    <a class="nav-link active" href="/Location/add">Add Hostel Location</a>
                </li>
            </ul>
        </div>
    <div class="card-body">
        
        <% if(data.length) { %>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#Hostel_ID</th>
                    <th scope="col">Longitude</th>
                    <th scope="col">Latitude</th>
                    <th width="200px"></th>
                </tr>
            </thead>
            <tbody>
            <% for(var i = 0; i< data.length; i++) { %>
                <tr>
                    <td><%= data[i].Hostel_ID %></td>
                    <td><%= data[i].Location[0]%></td>
                    <td><%= data[i].Location[1] %> </td>
                    <td>
                        <a class="btn btn-success edit" href="/location/edit/<%=data[i]._id%>">Edit</a>                       
                        <a class="btn btn-danger delete" onclick="return alert('Are You sure?')" href="/location/delete/<%=data[i]._id%>">Delete</a>                       
                    </td>
                </tr>
            <% } %>
            </tbody>
        </table>
        <% } %>
        
        <!-- if result is empty -->
        <% if(!data.length) { %>
            <p class="text-center">No Hostel Location found!</p>
        <% } %>

        </div>
    </div>
</div>